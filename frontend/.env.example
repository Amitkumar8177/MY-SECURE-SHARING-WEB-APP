# Frontend environment variables (Create React App)
# Copy to frontend/.env and adjust if necessary

# API base URL used by the frontend to reach the backend
REACT_APP_API_URL=http://localhost:5000

# Optionally, set other values like analytics or feature flags
# REACT_APP_FEATURE_X=true
